[project]
name = "my-worker"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["iii-sdk>=0.11.6"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
only-include = ["src/main.py"]
